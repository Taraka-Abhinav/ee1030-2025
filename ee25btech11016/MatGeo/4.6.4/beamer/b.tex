\documentclass{beamer}
\usepackage[utf8]{inputenc}

\usetheme{Madrid}
\usecolortheme{default}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\newcommand{\mat}[1]{\mathbf{#1}}
\usepackage{txfonts}
\usepackage{tkz-euclide}
\usepackage{listings}
\usepackage{adjustbox}
\usepackage{array}
\usepackage{tabularx}
\usepackage{gvv} 
\usepackage{lmodern}
\usepackage{circuitikz}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage[T1]{fontenc}
\usepackage{float}
\usepackage{hyperref}

% For 3D plots
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}

\lstset{
  language=Python,
  basicstyle=\ttfamily\small,
  breaklines=true,
  literate={λ}{{$\lambda$}}1
}

\setbeamertemplate{page number in head/foot}[totalframenumber]

\usepackage{tcolorbox}
\tcbuselibrary{minted,breakable,xparse,skins}

\definecolor{bg}{gray}{0.95}
\lstset{
    language=C,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    stringstyle=\color{orange},
    commentstyle=\color{green!60!black},
    numbers=left,
    numberstyle=\tiny\color{gray},
    breaklines=true,
    showstringspaces=false,
}
\begin{document}

\title
{4.6.4}
\date{October 3, 2025}

\author
{Taraka Abhinav - EE25BTECH11016}

\frame{\titlepage}

\begin{frame}{Question}
Find the equation of a line passing through the point $(2, 3, 2)$ and parallel to the line $\vec{r} = (-2\hat{i} + 3\hat{j}) + \lambda(2\hat{i} - 3\hat{j} + 6\hat{k})$. Also, find the distance between these two lines.
\end{frame}

\begin{frame}{Step 1: Equation of the Required Line}
The vector equation of a line is $\vec{r} = \vec{a_2} + \mu\vec{b}$.

The line passes through the point $(2, 3, 2)$, so the position vector is:
\begin{align}
    \vec{a_2} = \myvec{2 \\ 3 \\ 2}
\end{align}
The line is parallel to the given line, so they share the same direction vector:
\begin{align}
    \vec{b} = \myvec{2 \\ -3 \\ 6}
\end{align}
The equation of the required line is:
\begin{align}
    \vec{r} = \myvec{2 \\ 3 \\ 2} + \mu\myvec{2 \\ -3 \\ 6}
\end{align}
\end{frame}

\begin{frame}{Step 2: Distance Calculation}
The distance is found by identifying points $\mat{P} = \mat{a} + p_1\mat{d}$ and $\mat{Q} = \mat{b} + p_2\mat{d}$ on each line such that the vector $\mat{Q}-\mat{P}$ is orthogonal to the direction $\mat{d}$.

\subsubsection*{1. Define Vectors}
The problem defines the two parallel lines. We can represent them with the following vectors:
\begin{align}
\mat{a} &= \myvec{-2 \\ 3 \\ 0} & & \text{(Point on Line 1)} \\
\mat{b} &= \myvec{2 \\ 3 \\ 2} & & \text{(Point on Line 2)} \\
\mat{d} &= \myvec{2 \\ -3 \\ 6} & & \text{(Common Direction)}
\end{align}
\end{frame}

\begin{frame}{Step 2: Distance Calculation}
\subsubsection*{2. Solve for Parameters}
The orthogonality condition is $\mat{d}^\top(\mat{Q}-\mat{P}) = 0$. Expanding this gives the equation for the parameters:
\begin{align}
(\mat{d}^\top\mat{d})(p_2-p_1) + \mat{d}^\top(\mat{b}-\mat{a}) = 0
\end{align}
We calculate the scalar products:
\begin{align}
\mat{d}^\top\mat{d} &= \myvec{2 \\ -3 \\ 6}^\top \myvec{2 \\ -3 \\ 6} = 4 + 9 + 36 = 49 \\
\mat{d}^\top(\mat{b}-\mat{a}) &= \myvec{2 \\ -3 \\ 6}^\top \left( \myvec{2 \\ 3 \\ 2} - \myvec{-2 \\ 3 \\ 0} \right) \nonumber \\
&= \myvec{2 \\ -3 \\ 6}^\top \myvec{4 \\ 0 \\ 2} = 8 + 0 + 12 = 20
\end{align}
\end{frame}

\begin{frame}{Step 2: Distance Calculation}
Substituting these values back:
\begin{align}
49(p_2 - p_1) + 20 &= 0 \\
p_2 - p_1 &= -\frac{20}{49} \\
\implies p_1 - p_2 &= \frac{20}{49}
\end{align}
We need one pair of values that satisfy this. We can simply take $\mathbf{p_2 = 0}$, which gives $\mathbf{p_1 = 20/49}$.

\subsubsection*{3. Calculate the Distance}
Using these parameter values, we find the specific points $\mat{P}$ and $\mat{Q}$.
\begin{align}
\mat{P} &= \mat{a} + p_1\mat{d} = \myvec{-2 \\ 3 \\ 0} + \frac{20}{49}\myvec{2 \\ -3 \\ 6} = \myvec{-58/49 \\ 87/49 \\ 120/49} \\
\mat{Q} &= \mat{b} + p_2\mat{d} = \myvec{2 \\ 3 \\ 2}
\end{align}
\end{frame}

\begin{frame}{Step 2: Distance Calculation}
The vector connecting these points is:
\begin{align}
\mat{P}-\mat{Q} = \myvec{-58/49 - 98/49 \\ 87/49 - 147/49 \\ 120/49 - 98/49} = \frac{1}{49}\myvec{-156 \\ -60 \\ 22}
\end{align}
The distance is the norm of this vector, $||\mat{P}-\mat{Q}||$.
\begin{align}
d^2 &= \left\|\frac{1}{49}\myvec{-156 \\ -60 \\ 22}\right\|^2 \nonumber \\
&= \left(\frac{1}{49}\right)^2 \left( (-156)^2 + (-60)^2 + (22)^2 \right) \nonumber \\
&= \frac{1}{2401} (24336 + 3600 + 484) = \frac{28420}{2401} = \frac{580}{49} \\
d &= \sqrt{\frac{580}{49}} = \frac{\sqrt{4 \times 145}}{7}
\end{align}
\end{frame}

\begin{frame}[fragile]
    \frametitle{C Code}
    \begin{lstlisting}
#include <stdio.h>
#include <math.h>

int main() {
    double a1[3] = {-2, 3, 0};
    double a2[3] = {2, 3, 2};
    double b[3] = {2, -3, 6};
    double da[3], v[3];

    for (int i = 0; i < 3; i++) da[i] = a2[i] - a1[i];
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
\frametitle{C Code}
   \begin{lstlisting}
    v[0] = b[1]*da[2] - b[2]*da[1];
    v[1] = b[2]*da[0] - b[0]*da[2];
    v[2] = b[0]*da[1] - b[1]*da[0];

    double vnorm = sqrt(v[0]*v[0] + v[1]*v[1] + v[2]*v[2]);
    double bnorm = sqrt(b[0]*b[0] + b[1]*b[1] + b[2]*b[2]);

    double d = vnorm / bnorm;
    printf("Distance = %lf\n", d);

    return 0;
}
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
    \frametitle{Python + C Code}
    \begin{lstlisting}[language=Python]
import ctypes
import numpy as np
import matplotlib.pyplot as plt

# Load compiled shared library
lib = ctypes.CDLL('./line_distance.so')
lib.main()

# Given data
a1 = np.array([-2, 3, 0])
a2 = np.array([2, 3, 2])
b = np.array([2, -3, 6])

# Compute distance
da = a2 - a1
v = np.cross(b, da)
d = np.linalg.norm(v) / np.linalg.norm(b)
print("Distance =", d)
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
    \frametitle{Python + C Code}
    \begin{lstlisting}[language=Python]
# Projection of da onto b to find perpendicular foot
proj = (np.dot(da, b) / np.dot(b, b)) * b
perp_vec = da - proj
p1 = a1 + proj
p2 = a2

# Plot
fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')

# Line 1 and Line 2
t = np.linspace(-2, 2, 10)
line1 = a1 + np.outer(t, b)
line2 = a2 + np.outer(t, b)

ax.plot(line1[:,0], line1[:,1], line1[:,2], 'r', label='Line 1')
ax.plot(line2[:,0], line2[:,1], line2[:,2], 'b', label='Line 2')
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
    \frametitle{Python + C Code}
    \begin{lstlisting}[language=Python]
# Points with vector labels
ax.scatter(*a1, color='red')
ax.text(a1[0], a1[1], a1[2], f"A1 {a1}", color='red', fontsize=10)
ax.scatter(*a2, color='blue')
ax.text(a2[0], a2[1], a2[2], f"A2 {a2}", color='blue', fontsize=10)

# Perpendicular distance line
ax.plot([p1[0], p2[0]], [p1[1], p2[1]], [p1[2], p2[2]], 'g--', label=f'Distance = {round(d,3)}')

ax.legend()
plt.show()
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
    \frametitle{Python Plotting Code}
    \begin{lstlisting}[language=Python]
import numpy as np
import matplotlib.pyplot as plt

# Data
a1 = np.array([-2, 3, 0])
a2 = np.array([2, 3, 2])
b = np.array([2, -3, 6])

# Distance calculation
da = a2 - a1
v = np.cross(b, da)
d = np.linalg.norm(v) / np.linalg.norm(b)
print("Distance =", d)

# Projection for perpendicular line
proj = (np.dot(da, b) / np.dot(b, b)) * b
p1 = a1 + proj
p2 = a2
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
    \frametitle{Python Plotting Code}
    \begin{lstlisting}[language=Python]
# Plot
fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')

# Lines
t = np.linspace(-2, 2, 10)
line1 = a1 + np.outer(t, b)
line2 = a2 + np.outer(t, b)

ax.plot(line1[:,0], line1[:,1], line1[:,2], 'r', label='Line 1')
ax.plot(line2[:,0], line2[:,1], line2[:,2], 'b', label='Line 2')

# Points with labels
ax.scatter(*a1, color='red')
ax.text(a1[0], a1[1], a1[2], f"A1 {a1}", color='red', fontsize=10)
ax.scatter(*a2, color='blue')
ax.text(a2[0], a2[1], a2[2], f"A2 {a2}", color='blue', fontsize=10)
    \end{lstlisting}
\end{frame}

\begin{frame}[fragile]
    \frametitle{Python Plotting Code}
    \begin{lstlisting}[language=Python]
# Perpendicular distance line
ax.plot([p1[0], p2[0]], [p1[1], p2[1]], [p1[2], p2[2]], 'g--', label=f'Distance = {round(d,3)}')

# Direction vectors (as arrows from A1 and A2)
ax.quiver(a1[0], a1[1], a1[2], b[0], b[1], b[2], color='red', arrow_length_ratio=0.1, linewidth=1.5)
ax.text(a1[0]+b[0]*0.5, a1[1]+b[1]*0.5, a1[2]+b[2]*0.5, "b", color='red')

ax.quiver(a2[0], a2[1], a2[2], b[0], b[1], b[2], color='blue', arrow_length_ratio=0.1, linewidth=1.5)
ax.text(a2[0]+b[0]*0.5, a2[1]+b[1]*0.5, a2[2]+b[2]*0.5, "b", color='blue')

# Make plot readable
ax.legend()
ax.set_xlabel("X")
ax.set_ylabel("Y")
ax.set_zlabel("Z")

plt.show()
    \end{lstlisting}
\end{frame}

\begin{frame}
    \frametitle{Plot of the Parallel Lines}
    \centering
    \includegraphics[width=1\textwidth]{figs/Figure_1.png}   
\end{frame}

\end{document}